all:
  hosts:
    azure-master:
      ansible_host: "4.230.18.31"
      ansible_user: "azureuser"
      # บังคับล้างค่า ProxyCommand เพื่อไม่ให้ใช้ aws ssm ของ AWS และยกเลิก ControlMaster ชั่วคราวป้องกันการจดจำ session เก่า
      ansible_ssh_common_args: "-o ProxyCommand=none -o ControlMaster=no"

