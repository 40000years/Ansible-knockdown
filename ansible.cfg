[defaults]
host_key_checking = False
deprecation_warnings = False
stdout_callback = ansible.builtin.default
result_format = yaml
callbacks_enabled = timer, profile_tasks
# Roles path: รองรับทั้งแบบ role อยู่ใน root และ playbooks dir
roles_path = .:UBUNTU26-STIG/..

[ssh_connection]
# pipelining เร่งความเร็ว — ทำงานร่วมกับ SSH ได้ดี
pipelining = True
ssh_args = -o StrictHostKeyChecking=no -o ControlMaster=auto -o ControlPersist=60s

[inventory]
enable_plugins = amazon.aws.aws_ec2, host_list, ini, yaml
