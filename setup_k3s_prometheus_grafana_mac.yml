---
# ==============================================================
# K3s + Prometheus + Grafana Monitoring Playbook (Mac Master + AWS Workers)
#
# ใช้แทน setup_k3s_prometheus_grafana.yml
#
# Architecture:
#   - K3s Server (Master)          : Mac Local (Docker Container)
#   - K3s Agent (Workers)          : AWS EC2 Instances
#   - Prometheus + Grafana (Helm)  : รันบน Mac K3s Master
#
# วิธีใช้งานใน Semaphore:
#   ⚠️ เนื่องจาก playbook นี้ต้องใช้ 2 inventory
#     แนะนำให้แยกรันเป็น task:
#     Task 1: setup_k3s_mac_master.yml          (inventory.local_mac.yml)
#     Task 2: join_k3s_aws_workers.yml          (inventory.aws_ec2.yml)
#     Task 3: deploy_prometheus_grafana_mac.yml (inventory.local_mac.yml)
#
#   Extra Variables:
#     mac_master_ip: "<IP-ที่-AWS-เข้าถึงได้>"
# ==============================================================

# ==============================================================
# PLAY 1: ติดตั้ง K3s บน Mac Master (via Docker)
# ==============================================================
- name: "Play 1: Install K3s Server (Mac Master)"
  hosts: mac-master
  become: no
  gather_facts: true
  vars:
    mac_master_ip: "CHANGE_ME"

  tasks:
    - name: "Check if Docker is running"
      shell: "docker info > /dev/null 2>&1"
      register: docker_running
      ignore_errors: true
      changed_when: false

    - name: "Fail if Docker is not running"
      fail:
        msg: "❌ ไม่พบคำสั่ง docker หรือ Docker Daemon ไม่ได้เชื่อมต่อ! กรุณา rebuild Semaphore container โดยรัน 'cd Semaphore && docker compose up -d --build' และเปิด Docker Desktop"
      when: docker_running.rc != 0

    - name: "Check if K3s API port 6443 is responding"
      shell: "curl -k -s -o /dev/null -w '%{http_code}' https://127.0.0.1:6443"
      register: k3s_api_check
      ignore_errors: true
      changed_when: false

    - name: "Remove broken/outdated K3s container (if port 6443 is not responding)"
      shell: "docker rm -f k3s-server 2>/dev/null || true"
      when: k3s_api_check.stdout not in ['200', '401', '403', '404']

    - name: "Start K3s Server Docker container"
      shell: |
        docker run -d \
          --name k3s-server \
          --privileged \
          --restart unless-stopped \
          -p 6443:6443 \
          -p 32000:32000 \
          -p 32001:32001 \
          -p 32090:32090 \
          -v k3s-server-data:/var/lib/rancher/k3s \
          rancher/k3s:v1.30.2-k3s1 \
          server \
          --disable traefik \
          --write-kubeconfig-mode 644 \
          --tls-san {{ mac_master_ip }} \
          --tls-san 127.0.0.1 \
          --node-name mac-master
      when: k3s_api_check.stdout not in ['200', '401', '403', '404']

    - name: "Wait for K3s node-token to be ready"
      shell: "docker exec k3s-server cat /var/lib/rancher/k3s/server/node-token"
      register: k3s_token_cmd
      until: k3s_token_cmd.rc == 0 and (k3s_token_cmd.stdout | trim != "")
      retries: 30
      delay: 3
      changed_when: false

    - name: "Set k3s_token fact for workers to use"
      set_fact:
        k3s_token: "{{ k3s_token_cmd.stdout | trim }}"
        k3s_master_ip: "{{ mac_master_ip }}"

# ==============================================================
# PLAY 2: Join AWS Workers (ต้องใช้ combined inventory)
# ==============================================================
- name: "Play 2: Join K3s Agent (AWS Workers)"
  hosts: "all:!mac-master"
  become: yes
  gather_facts: true
  vars:
    mac_master_ip: "CHANGE_ME"

  tasks:
    - name: "Check if k3s agent is already installed"
      stat:
        path: /usr/local/bin/k3s
      register: k3s_binary

    - name: "Join K3s Cluster as Agent"
      shell: |
        curl -sfL https://get.k3s.io | \
          K3S_URL="https://{{ hostvars['mac-master']['k3s_master_ip'] }}:6443" \
          K3S_TOKEN="{{ hostvars['mac-master']['k3s_token'] }}" \
          INSTALL_K3S_EXEC="agent --node-name $(hostname -s)" \
          sh -
      when: not k3s_binary.stat.exists
      args:
        executable: /bin/bash

# ==============================================================
# PLAY 3: Deploy Prometheus + Grafana บน Mac Master (via Helm)
# ==============================================================
- name: "Play 3: Deploy Prometheus + Grafana Monitoring Stack"
  hosts: mac-master
  become: no
  gather_facts: true
  vars:
    mac_master_ip: "CHANGE_ME"
    grafana_admin_password: "Admin@Monitor123"
    grafana_nodeport: 32000
    prometheus_nodeport: 32001

  tasks:
    - name: "Install Helm"
      shell: |
        if ! command -v helm &>/dev/null; then
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
        fi
      args:
        executable: /bin/bash

    - name: "Add Prometheus Helm repo"
      shell: |
        docker exec k3s-server helm repo add prometheus-community https://prometheus-community.github.io/helm-charts 2>/dev/null || \
        helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
        helm repo update
      args:
        executable: /bin/bash

    - name: "Wait for all K3s nodes to be Ready"
      shell: |
        docker exec k3s-server kubectl wait --for=condition=Ready nodes --all --timeout=180s
      retries: 5
      delay: 30
      register: nodes_ready
      until: nodes_ready.rc == 0

    - name: "Deploy kube-prometheus-stack (Prometheus + Grafana + Alertmanager)"
      shell: |
        docker exec k3s-server kubectl create namespace monitoring 2>/dev/null || true
        docker exec k3s-server helm upgrade --install monitoring prometheus-community/kube-prometheus-stack \
          --namespace monitoring \
          --set grafana.adminPassword="{{ grafana_admin_password }}" \
          --set grafana.service.type=NodePort \
          --set grafana.service.nodePort={{ grafana_nodeport }} \
          --set prometheus.service.type=NodePort \
          --set prometheus.service.nodePort={{ prometheus_nodeport }} \
          --set prometheus.prometheusSpec.scrapeInterval=15s \
          --set prometheus.prometheusSpec.evaluationInterval=15s \
          --wait \
          --timeout 10m
      args:
        executable: /bin/bash

    - name: "Wait for Grafana pod to be Running"
      shell: |
        docker exec k3s-server kubectl wait --for=condition=Ready pod \
          -l app.kubernetes.io/name=grafana \
          -n monitoring \
          --timeout=300s
      retries: 5
      delay: 20
      register: grafana_ready
      until: grafana_ready.rc == 0

    - name: "Show Monitoring Endpoints"
      debug:
        msg:
          - "============================================"
          - "  Monitoring Stack is LIVE on Mac!"
          - "============================================"
          - "  Grafana   : http://localhost:{{ grafana_nodeport }}"
          - "  Prometheus: http://localhost:{{ prometheus_nodeport }}"
          - "  Username  : admin"
          - "  Password  : {{ grafana_admin_password }}"
          - "============================================"
          - "  ไม่ต้อง SSH Tunnel — เข้าถึงได้ทันทีจาก Mac"
          - "============================================"
          - "  Pre-built dashboards:"
          - "  - Node Exporter Full (ID: 1860)"
          - "  - K3s Cluster (ID: 13770)"
          - "============================================"
