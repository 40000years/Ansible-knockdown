---
# ==============================================================
# K3s + Prometheus + Grafana Monitoring Playbook
# Architecture (ใช้ instance_id จาก AWS EC2 Inventory):
#   - k3s_master_id: Instance แรกใน group ที่กำหนด หรือกำหนดเองผ่าน Extra Vars
#   - Workers:       Instances ที่เหลือทั้งหมด
#
# วิธีใช้งานใน Semaphore:
#   Extra Variables:
#     k3s_master_id: "i-0xxxxxxxxxxxxxxxxx"  <- ใส่ instance_id ของเครื่องที่จะเป็น Master
# ==============================================================

# ==============================================================
# PLAY 1: ติดตั้ง K3s บน Master Node
# ==============================================================
- name: "Play 1: Install K3s Server (Master Node)"
  hosts: "{{ k3s_master_id }}"
  become: yes
  gather_facts: true
  vars:
    k3s_master_id: "CHANGE_ME"  # Override ผ่าน Semaphore Extra Variables

  tasks:
    - name: "Check if k3s is already installed"
      stat:
        path: /usr/local/bin/k3s
      register: k3s_binary

    - name: "Install K3s Server"
      shell: |
        curl -sfL https://get.k3s.io | \
          INSTALL_K3S_EXEC="server \
            --disable traefik \
            --write-kubeconfig-mode 644 \
            --node-name $(hostname -s)" \
          sh -
      when: not k3s_binary.stat.exists
      args:
        executable: /bin/bash

    - name: "Wait for K3s to be ready"
      wait_for:
        path: /var/lib/rancher/k3s/server/node-token
        timeout: 120

    - name: "Read K3s node-token"
      slurp:
        src: /var/lib/rancher/k3s/server/node-token
      register: k3s_token_raw

    - name: "Set k3s_token fact for workers to use"
      set_fact:
        k3s_token: "{{ k3s_token_raw.content | b64decode | trim }}"
        k3s_master_ip: "{{ ansible_default_ipv4.address }}"

    # Expose facts ไปยัง dummy host เพื่อให้ Play 2 เรียกใช้ได้
    - name: "Share master facts to dummy host"
      add_host:
        name: "k3s_master_facts"
        k3s_token: "{{ k3s_token }}"
        k3s_master_ip: "{{ ansible_default_ipv4.address }}"

# ==============================================================
# PLAY 2: ติดตั้ง K3s บน Worker Nodes
# ==============================================================
- name: "Play 2: Join K3s Agent (Worker Nodes)"
  hosts: "all:!{{ k3s_master_id }}"
  become: yes
  gather_facts: true
  vars:
    k3s_master_id: "CHANGE_ME"

  tasks:
    - name: "Check if k3s agent is already installed"
      stat:
        path: /usr/local/bin/k3s
      register: k3s_binary

    - name: "Join K3s Cluster as Agent"
      shell: |
        curl -sfL https://get.k3s.io | \
          K3S_URL="https://{{ hostvars['k3s_master_facts']['k3s_master_ip'] }}:6443" \
          K3S_TOKEN="{{ hostvars['k3s_master_facts']['k3s_token'] }}" \
          INSTALL_K3S_EXEC="agent --node-name $(hostname -s)" \
          sh -
      when: not k3s_binary.stat.exists
      args:
        executable: /bin/bash

# ==============================================================
# PLAY 3: Deploy Prometheus + Grafana บน Master (via Helm)
# ==============================================================
- name: "Play 3: Deploy Prometheus + Grafana Monitoring Stack"
  hosts: "{{ k3s_master_id }}"
  become: yes
  gather_facts: true
  vars:
    k3s_master_id: "CHANGE_ME"
    grafana_admin_password: "Admin@Monitor123"
    grafana_nodeport: 32000
    prometheus_nodeport: 32001

  tasks:
    - name: "Install Helm"
      shell: |
        if ! command -v helm &>/dev/null; then
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
        fi
      args:
        executable: /bin/bash

    - name: "Add Prometheus Helm repo"
      shell: |
        export KUBECONFIG=/etc/rancher/k3s/k3s.yaml
        helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
        helm repo update
      args:
        executable: /bin/bash
      environment:
        KUBECONFIG: /etc/rancher/k3s/k3s.yaml

    - name: "Wait for all K3s nodes to be Ready"
      shell: |
        kubectl wait --for=condition=Ready nodes --all --timeout=180s
      environment:
        KUBECONFIG: /etc/rancher/k3s/k3s.yaml
      retries: 5
      delay: 30
      register: nodes_ready
      until: nodes_ready.rc == 0

    - name: "Deploy kube-prometheus-stack (Prometheus + Grafana + Alertmanager)"
      shell: |
        helm upgrade --install monitoring prometheus-community/kube-prometheus-stack \
          --namespace monitoring \
          --create-namespace \
          --set grafana.adminPassword="{{ grafana_admin_password }}" \
          --set grafana.service.type=NodePort \
          --set grafana.service.nodePort={{ grafana_nodeport }} \
          --set prometheus.service.type=NodePort \
          --set prometheus.service.nodePort={{ prometheus_nodeport }} \
          --set prometheus.prometheusSpec.scrapeInterval=15s \
          --set prometheus.prometheusSpec.evaluationInterval=15s \
          --wait \
          --timeout 10m
      environment:
        KUBECONFIG: /etc/rancher/k3s/k3s.yaml
      args:
        executable: /bin/bash

    - name: "Wait for Grafana pod to be Running"
      shell: |
        kubectl wait --for=condition=Ready pod \
          -l app.kubernetes.io/name=grafana \
          -n monitoring \
          --timeout=300s
      environment:
        KUBECONFIG: /etc/rancher/k3s/k3s.yaml
      retries: 5
      delay: 20
      register: grafana_ready
      until: grafana_ready.rc == 0

    - name: "Show Monitoring Endpoints"
      debug:
        msg:
          - "============================================"
          - "  Monitoring Stack is LIVE!"
          - "============================================"
          - "  Grafana   : http://{{ ansible_default_ipv4.address }}:{{ grafana_nodeport }}"
          - "  Prometheus: http://{{ ansible_default_ipv4.address }}:{{ prometheus_nodeport }}"
          - "  Username  : admin"
          - "  Password  : {{ grafana_admin_password }}"
          - "============================================"
          - "  Pre-built dashboards:"
          - "  - Node Exporter Full (ID: 1860)"
          - "  - K3s Cluster (ID: 13770)"
          - "============================================"
